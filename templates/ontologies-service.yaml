apiVersion: v1
kind: Service
metadata:
  name: archivo
  labels:
    servicio: archivo
  namespace: {{ .Release.Namespace | quote }}
spec:
  ports:
    - name: "port-ontologias"
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: ontologias
status:
  loadBalancer: {}