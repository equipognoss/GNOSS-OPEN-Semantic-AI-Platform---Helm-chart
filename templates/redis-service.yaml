apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: redis
spec:
  type: ClusterIP
  ports:
  - name: redis
    protocol: TCP
    port: 6379
    targetPort: 6379
  selector:
    app: redis