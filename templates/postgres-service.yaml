apiVersion: v1
kind: Service
metadata:
  name: "postgres"
  namespace: {{ .Release.Namespace | quote }}
  labels:
    app: postgres
spec:
  type: ClusterIP
  ports:
  - name: sql
    protocol: TCP
    port: 5432
    targetPort: 5432
  selector:
    app: postgres