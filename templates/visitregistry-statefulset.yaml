apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app: visitregistry
  name: visitregistry
  namespace: {{ .Release.Namespace | quote }}
spec:
  replicas: {{ .Values.generalBack.replicas }}
  selector:
    matchLabels:
      app: visitregistry
  template:
    metadata:
      labels:
        app: visitregistry
    spec:
      {{ if or (.Values.visitregistry.tolerations) (.Values.general.extraTolerations) }}
      tolerations:
      {{- with .Values.visitregistry.tolerations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.general.extraTolerations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- end }}
      {{- $cacheAffinity := default dict .Values.visitregistry.affinity }}
      {{- $extraAffinity := default dict .Values.general.extraAffinity }}
      {{- $mergedAffinity := merge $cacheAffinity $extraAffinity }}
      {{- if $mergedAffinity }}
      affinity:
      {{ toYaml $mergedAffinity | nindent 8 }}
      {{- end }}
      securityContext:
        fsGroupChangePolicy: OnRootMismatch
      {{- if .Values.global.serviceAccountName }}
      serviceAccountName: {{  .Values.global.serviceAccountName | quote }}
      {{- end }}
      {{- with .Values.general.imagePullSecrets }}
      imagePullSecrets:
      {{ toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: volumepermissions
          image: busybox
          command:
            - sh
            - '-c'
            - |
              chmod -R 777 /app/recursos
              chmod -R 777 /app/Votos
              chmod -R 777 /app/Visitas
              chmod -R 777 /app/Comentarios 

              echo 'Volume permissions OK'
          resources: {}
          volumeMounts:
            - name: vol-gnoss
              mountPath: /app/recursos
              subPath: sockets/Recursos
            - name: vol-gnoss
              mountPath: /app/Votos
              subPath: sockets/Votos
            - name: vol-gnoss
              mountPath: /app/Visitas
              subPath: sockets/Visitas
            - name: vol-gnoss
              mountPath: /app/Comentarios
              subPath: sockets/Comentarios
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          imagePullPolicy: IfNotPresent
      containers:
      - name: visitregistry
        image: {{ printf "%s:%s" .Values.visitregistry.image .Values.visitregistry.tag }}
        imagePullPolicy: {{ .Values.generalBack.pullPolicy }}
        {{- if .Values.visitregistry.limitsEnabled | quote }}
        resources:
          limits:
            memory: {{ .Values.visitregistry.limitMemory }}
            cpu: {{ .Values.visitregistry.limitCpu }}
          requests:
            memory: {{ .Values.visitregistry.requestMemory }}
            cpu: {{ .Values.visitregistry.requestCpu }}
        {{- end }}
        envFrom:
          - configMapRef:
              name: config-gnoss-core
        env:
        - name: acid
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.sql.name }}
              key: {{ .Values.secrets.sql.acid.secretKey }}
        - name: base
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.sql.name }}
              key: {{ .Values.secrets.sql.base.secretKey }}
        - name: virtuosoConnectionString
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.virtuoso.name }}
              key: {{ .Values.secrets.virtuoso.read.secretKey }}
        - name: Virtuoso__Escritura__Virtuoso1
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.virtuoso.name }}
              key: {{ .Values.secrets.virtuoso.write1.secretKey }}
        - name: Virtuoso__Escritura__Virtuoso2
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.virtuoso.name }}
              key: {{ .Values.secrets.virtuoso.write2.secretKey }}
        - name: RabbitMQ__colaServiciosWin
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.rabbitmq.name }}
              key: {{ .Values.secrets.rabbitmq.connectionString.secretKey }}
        - name: RabbitMQ__colaReplicacion
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.rabbitmq.name }}
              key: {{ .Values.secrets.rabbitmq.connectionString.secretKey }}
        - name: intervalo
          value: "100"
  volumeClaimTemplates:
  - metadata:
      name: vol-gnoss
      namespace: {{ .Release.Namespace | quote }}
    spec:
      accessModes: [ "ReadWriteOnce" ]
      storageClassName: {{ .Values.global.storageClassName }}
      resources:
        requests:
          storage: {{ .Values.visitregistry.espacioVol }}