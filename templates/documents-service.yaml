apiVersion: v1
kind: Service
metadata:
  name: documents
  labels:
    servicio: documents
  namespace: {{ .Release.Namespace | quote }}
spec:
  ports:
    - name: "port-document"
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: documents
status:
  loadBalancer: {}