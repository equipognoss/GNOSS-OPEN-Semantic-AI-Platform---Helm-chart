apiVersion: v1
kind: Service
metadata:
  labels:
    servicio: facetas
  name: facetas
  namespace: {{ .Release.Namespace | quote }}
spec:
  ports:
    - name: "port-facetas"
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: facetas
status:
  loadBalancer: {}