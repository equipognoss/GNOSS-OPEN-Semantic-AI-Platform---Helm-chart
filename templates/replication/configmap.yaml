{{- if .Values.replication.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: replication
data:
  appsettings.json: |
    {
      "Logging": {
        "LogLevel": {
          "Default": "Error"
        }
      },
      "Serilog": {
        "MinimumLevel": {
          "Default": {{ .Values.replication.logLevel.default | quote }},
          "Override": {
            {{- $first := true }}
            {{- range $namespace, $level := .Values.replication.logLevel.overrides }}
            {{- if not $first }},{{ end }}
            {{ $namespace | quote }}: {{ $level | quote }}
            {{- $first = false }}
            {{- end }}
          }
        }
      }
    }
{{- end }}