apiVersion: v1
kind: Service
metadata:
  name: identityserver
  labels:
    servicio: identityserver
  namespace: {{ .Release.Namespace | quote }}
spec:
  ports:
    - name: "port-identity"
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: identityserver
status:
  loadBalancer: {}