apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-np
  namespace: {{ .Release.Namespace | quote }}
spec:
  type: NodePort
  sessionAffinity: None
  ports:
  - name: amqp
    protocol: TCP
    port: 5672
    targetPort: 5672
  selector:
    app: rabbitmq
