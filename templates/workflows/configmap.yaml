{{- if .Values.workflows.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: workflows
data:
  appsettings.json: |
    {
      "Logging": {
        "LogLevel": {
          "Default": "Error"
        }
      },
      "Serilog": {
        "MinimumLevel": {
          "Default": {{ .Values.workflows.logLevel.default | quote }},
          "Override": {
           {{- $first := true }}
            {{- range $namespace, $level := .Values.workflows.logLevel.overrides }}
            {{- if not $first }},{{ end }}
            {{ $namespace | quote }}: {{ $level | quote }}
            {{- $first = false }}
            {{- end }}
          }
        }
      }
    }
{{- end }}