apiVersion: v1
kind: Service
metadata:
  name: static
  labels:
    servicio: static
  namespace: {{ .Release.Namespace | quote }}
spec:
  ports:
    - name: "port-static"
      protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
    app: static
status:
  loadBalancer: {}